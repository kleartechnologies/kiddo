# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
# ...except the placeholder template the README points at (no secrets).
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# local env (NEXT_PUBLIC_SITE_URL, future secrets)
.env*.local

# Firebase emulator logs
*-debug.log

# Firebase deploy/hosting cache
.firebase/

# Claude Code local runtime state (session locks, machine-local settings)
.claude/scheduled_tasks.lock
.claude/settings.local.json

# Local Netlify folder
.netlify

# The Netlify CLI's Deno cache for the edge-function bootstrap
deno.lock
