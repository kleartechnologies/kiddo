# /public/games

Optional key art for a game card's illustration bed (landscape, roughly
640×400). Set `art` on the game in `src/data/games.ts` to use it instead of the
character line-up:

`art: { src: "/games/memory-match.webp", width: 640, height: 400 }`
