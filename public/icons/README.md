# /public/icons

App icons, install icons and any custom pictograms that are not available in
Lucide. Interface icons should come from `lucide-react` first — only add files
here when the icon is genuinely part of the KIDDO brand.

The browser tab icon is generated from `src/app/icon.svg`.
